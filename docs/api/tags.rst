.. _tags_api:

User-Defined Tags
=================

Each |Slide| object has a |UserDefinedTags| object accessed via its
:attr:`tags` property. Tags are key-value string pairs that persist with the
slide through save/load cycles and are useful for embedding application-specific
metadata.

|UserDefinedTags| objects
-------------------------

.. autoclass:: pptx.tags.UserDefinedTags()
   :members:
   :undoc-members:
